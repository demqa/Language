Danse Macabre
{
    y = 10;

    Forte FUCK(y, x+y+z, 64*FUCK(x));

    Fine (a * b) * c + d + e * f;
}

Opus FUCK(x, y, z)
{
   Piacere (x <= 1) Deciso
   {
       Fine 1;
   }

   Fine x * FUCK(x - 1);
}
